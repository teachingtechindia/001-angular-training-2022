import { CustomNgIfDirective } from './custom-ng-if.directive';

describe('CustomNgIfDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomNgIfDirective();
    expect(directive).toBeTruthy();
  });
});
